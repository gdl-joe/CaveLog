# Node-Module (Entwickler-Abhängigkeiten)
frontend/node_modules/

# Sensible Konfiguration — niemals ins Repo!
.env
.env.local
.env.*.local

# Echte Nutzerdaten — niemals ins Repo!
uploads/
deploy/uploads/
deploy/db/*.db
deploy/db/*.db-shm
deploy/db/*.db-wal

# Temporäre Dateien
.DS_Store
*.log
/tmp/
*.cache
